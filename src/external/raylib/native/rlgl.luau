--!strict
--!optimize 2
--!native

local raylib = require("./raylib")
local ffi = _G.zune.ffi

type float = number
type int = number
type uint = number
type bool = boolean
type FFIPointer = any
type Matrix = raylib.Matrix

local void = ffi.types.void
local i32 = ffi.types.i32
local u32 = ffi.types.u32
local u8 = ffi.types.u8
local f32 = ffi.types.float
local ptr = ffi.types.pointer
local f64 = ffi.types.double

type long = buffer
type double = number
type cstring = string | buffer | any

local function fn(returns: any, args: { any })
	return {
		returns = returns,
		args = args,
	}
end

export type Fns = {
	rlEnableDepthTest: () -> (),
	rlDisableDepthTest: () -> (),
	rlEnableDepthMask: () -> (),
	rlDisableDepthMask: () -> (),
	rlEnableBackfaceCulling: () -> (),
	rlDisableBackfaceCulling: () -> (),
	rlLoadFramebuffer: (width: int, height: int) -> uint,
	rlEnableFramebuffer: (id: uint) -> (),
	rlDisableFramebuffer: () -> (),
	rlLoadTexture: (data: FFIPointer, width: int, height: int, format: int, mipmapCount: int) -> uint,
	rlLoadTextureDepth: (width: int, height: int, noRed: bool) -> uint,
	rlFramebufferAttach: (fboId: uint, texId: uint, attachType: int, texType: int, mipLevel: int) -> (),
	rlFramebufferComplete: (id: uint) -> bool,
	rlUnloadFramebuffer: (id: uint) -> (),
	rlUnloadTexture: (id: uint) -> (),
	rlSetClipPlanes: (near: float, far: float) -> (),
	rlActiveDrawBuffers: (count: int) -> (),
	rlGetMatrixProjection: () -> Matrix,
	rlGetMatrixModelview: () -> Matrix,
	rlSetBlendMode: (mode: int) -> (),
}

return {
	const = {
		-- Framebuffer attachment type
		RL_ATTACHMENT_COLOR_CHANNEL0 = 0,
		RL_ATTACHMENT_COLOR_CHANNEL1 = 1,
		RL_ATTACHMENT_COLOR_CHANNEL2 = 2,
		RL_ATTACHMENT_COLOR_CHANNEL3 = 3,
		RL_ATTACHMENT_COLOR_CHANNEL4 = 4,
		RL_ATTACHMENT_COLOR_CHANNEL5 = 5,
		RL_ATTACHMENT_COLOR_CHANNEL6 = 6,
		RL_ATTACHMENT_COLOR_CHANNEL7 = 7,
		RL_ATTACHMENT_DEPTH = 100,
		RL_ATTACHMENT_STENCIL = 101,

		-- Framebuffer texture type
		RL_ATTACHMENT_CUBEMAP_POSITIVE_X = 0,
		RL_ATTACHMENT_CUBEMAP_NEGATIVE_X = 1,
		RL_ATTACHMENT_CUBEMAP_POSITIVE_Y = 2,
		RL_ATTACHMENT_CUBEMAP_NEGATIVE_Y = 3,
		RL_ATTACHMENT_CUBEMAP_POSITIVE_Z = 4,
		RL_ATTACHMENT_CUBEMAP_NEGATIVE_Z = 5,
		RL_ATTACHMENT_TEXTURE2D = 100,
		RL_ATTACHMENT_RENDERBUFFER = 200,
	},
	def = {
		rlMatrixMode = fn(void, { i32 }),
		rlPushMatrix = fn(void, {}),
		rlPopMatrix = fn(void, {}),
		rlLoadIdentity = fn(void, {}),
		rlTranslatef = fn(void, { f32, f32, f32 }),
		rlRotatef = fn(void, { f32, f32, f32, f32 }),
		rlScalef = fn(void, { f32, f32, f32 }),
		rlMultMatrixf = fn(void, { ptr }),
		rlFrustum = fn(void, { f64, f64, f64, f64, f64, f64 }),
		rlOrtho = fn(void, { f64, f64, f64, f64, f64, f64 }),
		rlViewport = fn(void, { i32, i32, i32, i32 }),
		rlSetClipPlanes = fn(void, { f64, f64 }),
		rlGetCullDistanceNear = fn(f64, {}),
		rlGetCullDistanceFar = fn(f64, {}),
		rlBegin = fn(void, { i32 }),
		rlEnd = fn(void, {}),
		rlVertex2i = fn(void, { i32, i32 }),
		rlVertex2f = fn(void, { f32, f32 }),
		rlVertex3f = fn(void, { f32, f32, f32 }),
		rlTexCoord2f = fn(void, { f32, f32 }),
		rlNormal3f = fn(void, { f32, f32, f32 }),
		rlColor4ub = fn(void, { u8, u8, u8, u8 }),
		rlColor3f = fn(void, { f32, f32, f32 }),
		rlColor4f = fn(void, { f32, f32, f32, f32 }),
		rlEnableVertexArray = fn(u8, { u32 }),
		rlDisableVertexArray = fn(void, {}),
		rlEnableVertexBuffer = fn(void, { u32 }),
		rlDisableVertexBuffer = fn(void, {}),
		rlEnableVertexBufferElement = fn(void, { u32 }),
		rlDisableVertexBufferElement = fn(void, {}),
		rlEnableVertexAttribute = fn(void, { u32 }),
		rlDisableVertexAttribute = fn(void, { u32 }),
		rlActiveTextureSlot = fn(void, { i32 }),
		rlEnableTexture = fn(void, { u32 }),
		rlDisableTexture = fn(void, {}),
		rlEnableTextureCubemap = fn(void, { u32 }),
		rlDisableTextureCubemap = fn(void, {}),
		rlTextureParameters = fn(void, { u32, i32, i32 }),
		rlCubemapParameters = fn(void, { u32, i32, i32 }),
		rlEnableShader = fn(void, { u32 }),
		rlDisableShader = fn(void, {}),
		rlEnableFramebuffer = fn(void, { u32 }),
		rlDisableFramebuffer = fn(void, {}),
		rlGetActiveFramebuffer = fn(i32, {}),
		rlActiveDrawBuffers = fn(void, { i32 }),
		rlBlitFramebuffer = fn(void, { i32, i32, i32, i32, i32, i32, i32, i32, i32 }),
		rlBindFramebuffer = fn(void, { u32, u32 }),
		rlEnableColorBlend = fn(void, {}),
		rlDisableColorBlend = fn(void, {}),
		rlEnableDepthTest = fn(void, {}),
		rlDisableDepthTest = fn(void, {}),
		rlEnableDepthMask = fn(void, {}),
		rlDisableDepthMask = fn(void, {}),
		rlEnableBackfaceCulling = fn(void, {}),
		rlDisableBackfaceCulling = fn(void, {}),
		rlColorMask = fn(void, { u8, u8, u8, u8 }),
		rlSetCullFace = fn(void, { i32 }),
		rlEnableScissorTest = fn(void, {}),
		rlDisableScissorTest = fn(void, {}),
		rlScissor = fn(void, { i32, i32, i32, i32 }),
		rlEnableWireMode = fn(void, {}),
		rlDisableWireMode = fn(void, {}),
		rlSetLineWidth = fn(void, { f32 }),
		rlGetLineWidth = fn(f32, {}),
		rlEnableSmoothLines = fn(void, {}),
		rlDisableSmoothLines = fn(void, {}),
		rlEnableStereoRender = fn(void, {}),
		rlDisableStereoRender = fn(void, {}),
		rlIsStereoRenderEnabled = fn(u8, {}),
		rlClearScreenBuffers = fn(void, {}),
		rlCheckErrors = fn(void, {}),
		rlSetBlendMode = fn(void, { i32 }),
		rlSetBlendFactors = fn(void, { i32, i32, i32 }),
		rlSetBlendFactorsSeparate = fn(void, { i32, i32, i32, i32, i32, i32 }),
		rlglInit = fn(void, { i32, i32 }),
		rlglClose = fn(void, {}),
		rlLoadExtensions = fn(void, {}),
		rlGetVersion = fn(i32, {}),
		rlSetFramebufferWidth = fn(void, { i32 }),
		rlGetFramebufferWidth = fn(i32, {}),
		rlSetFramebufferHeight = fn(void, { i32 }),
		rlGetFramebufferHeight = fn(i32, {}),
		rlGetTextureIdDefault = fn(i32, {}),
		rlGetShaderIdDefault = fn(i32, {}),
		rlLoadRenderBatch = fn(ptr, { i32, i32 }),
		rlUnloadRenderBatch = fn(void, { ptr }),
		rlDrawRenderBatch = fn(void, { ptr }),
		rlSetRenderBatchActive = fn(void, { ptr }),
		rlDrawRenderBatchActive = fn(void, {}),
		rlCheckRenderBatchLimit = fn(u8, { i32 }),
		rlSetTexture = fn(void, { u32 }),
		rlLoadVertexArray = fn(i32, {}),
		rlLoadVertexBuffer = fn(i32, { ptr, i32, u8 }),
		rlLoadVertexBufferElement = fn(i32, { ptr, i32, u8 }),
		rlUpdateVertexBuffer = fn(void, { u32, ptr, i32, i32 }),
		rlUpdateVertexBufferElements = fn(void, { u32, ptr, i32, i32 }),
		rlUnloadVertexArray = fn(void, { u32 }),
		rlUnloadVertexBuffer = fn(void, { u32 }),
		rlSetVertexAttribute = fn(void, { u32, i32, i32, u8, i32, i32 }),
		rlSetVertexAttributeDivisor = fn(void, { u32, i32 }),
		rlSetVertexAttributeDefault = fn(void, { i32, ptr, i32, i32 }),
		rlDrawVertexArray = fn(void, { i32, i32 }),
		rlDrawVertexArrayElements = fn(void, { i32, i32, ptr }),
		rlDrawVertexArrayInstanced = fn(void, { i32, i32, i32 }),
		rlDrawVertexArrayElementsInstanced = fn(void, { i32, i32, ptr, i32 }),
		rlLoadTexture = fn(i32, { ptr, i32, i32, i32, i32 }),
		rlLoadTextureDepth = fn(i32, { i32, i32, u8 }),
		rlLoadTextureCubemap = fn(i32, { ptr, i32, i32, i32 }),
		rlUpdateTexture = fn(void, { u32, i32, i32, i32, i32, i32, ptr }),
		rlGetGlTextureFormats = fn(void, { i32, u32, u32, u32 }),
		rlUnloadTexture = fn(void, { u32 }),
		rlGenTextureMipmaps = fn(void, { u32, i32, i32, i32, i32 }),
		rlLoadFramebuffer = fn(u32, { i32, i32 }),
		rlFramebufferAttach = fn(void, { u32, u32, i32, i32, i32 }),
		rlFramebufferComplete = fn(u8, { u32 }),
		rlUnloadFramebuffer = fn(void, { u32 }),
		rlLoadShaderProgram = fn(i32, { u32, u32 }),
		rlUnloadShaderProgram = fn(void, { u32 }),
		rlGetLocationUniform = fn(i32, { u32, ptr }),
		rlGetLocationAttrib = fn(i32, { u32, ptr }),
		rlSetUniform = fn(void, { i32, ptr, i32, i32 }),
		rlSetUniformMatrix = fn(void, { i32, ptr }),
		rlSetUniformMatrices = fn(void, { i32, ptr, i32 }),
		rlSetUniformSampler = fn(void, { i32, u32 }),
		rlSetShader = fn(void, { u32, i32 }),
		rlComputeShaderDispatch = fn(void, { u32, u32, u32 }),
		rlLoadShaderBuffer = fn(i32, { u32, ptr, i32 }),
		rlUnloadShaderBuffer = fn(void, { u32 }),
		rlUpdateShaderBuffer = fn(void, { u32, ptr, u32, u32 }),
		rlBindShaderBuffer = fn(void, { u32, u32 }),
		rlReadShaderBuffer = fn(void, { u32, u32, u32 }),
		rlCopyShaderBuffer = fn(void, { u32, u32, u32, u32, u32 }),
		rlGetShaderBufferSize = fn(i32, { u32 }),
		rlBindImageTexture = fn(void, { u32, u32, i32, u8 }),
		rlGetMatrixModelview = fn(raylib.structs.Matrix, {}),
		rlGetMatrixProjection = fn(raylib.structs.Matrix, {}),
		rlGetMatrixTransform = fn(raylib.structs.Matrix, {}),
		rlGetMatrixProjectionStereo = fn(raylib.structs.Matrix, { i32 }),
		rlGetMatrixViewOffsetStereo = fn(raylib.structs.Matrix, { i32 }),
		rlSetMatrixProjection = fn(void, { raylib.structs.Matrix }),
		rlSetMatrixModelview = fn(void, { raylib.structs.Matrix }),
		rlSetMatrixProjectionStereo = fn(void, { raylib.structs.Matrix, raylib.structs.Matrix }),
		rlSetMatrixViewOffsetStereo = fn(void, { raylib.structs.Matrix, raylib.structs.Matrix }),
		rlLoadDrawCube = fn(void, {}),
		rlLoadDrawQuad = fn(void, {}),
	},
}
